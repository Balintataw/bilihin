# Expense Tracker Bilihin

A mysterious Flutter project.

## key commands

build split architechure apks `flutter build apk --split-per-abi`
install apk locally `flutter install`
build appbundle `flutter build appbundle`

